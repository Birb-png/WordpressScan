# WordpressScan
ACS Wordpress Scan via CVE vuls
:::::::::::::::::::::::
To run the web 
run the app.py but don't forget to $env:WPSCAN_API_TOKEN = 'YOUR_TOKEN' in powershell
or set WPSCAN_API_TOKEN = YOUR_TOKEN in cmd
or make your own .env file
